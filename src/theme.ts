import { createTheme } from '@mui/material';

export const theme = createTheme({
  typography: {
    fontFamily: 'Outfit, sans-serif;'
  },
  palette: {
    primary: {
      main: '#6f6af8',
      light: '#f8f8ff'
    },
    secondary: {
      main: '#ff4600',
      dark: '#ff0076'
    }
  }
});
