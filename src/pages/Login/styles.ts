import type { Styles } from 'types';

const styles: Styles = {
  root: {
    paddingTop: 40,
    textAlign: 'center'
  },
  title: {
    marginBottom: 20
  },
  input: {
    marginBottom: 20
  }
};

export default styles;
