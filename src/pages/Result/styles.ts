import type { Styles } from "types";

const styles: Styles = {
  root: {
    paddingTop: 40,
    textAlign: "center",
  },
  header: {
    width: "100%",
    height: 30,
  },
};

export default styles;
